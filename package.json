{
  "name": "stan-robotix-kanban",
  "version": "1.0.0",
  "description": "Multi-user Kanban board customized for STAN ROBOTIX, optimized for Raspberry Pi",
  "main": "backend/index.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently \"npm run dev-backend\" \"npm run dev-frontend\"",
    "dev-backend": "node backend/index.js --dev",
    "dev-frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/index.js"
  },
  "keywords": [
    "kanban",
    "raspberry-pi",
    "multi-user",
    "express",
    "react"
  ],
  "author": "Antigravity",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
